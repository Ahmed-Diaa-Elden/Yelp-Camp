<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editing <%= camp.title %> </title>
</head>
<body>
    <form action="/campgrounds/<%= camp._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="campgrounds[title]" value="<%= camp.title %>">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="campgrounds[location]" value="<%= camp.location %>">
        </div>
        <button>Save</button>
    </form>
    <footer>
        <a href="/campgrounds/<%= camp._id %> ">Back to Campground</a>
    </footer>
</body>
</html>